<UserControl x:Class="MageEditor.Content.ContentBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MageEditor.Content"
             d:DataContext="{d:DesignInstance Type=local:ContentBrowser}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <local:DataSizeToStringConverter x:Key="dataSizeToStringConverter"/>

        <Style x:Key="SmallIconStyle" TargetType="{x:Type Image}">
            <Setter Property="Source" Value="{Binding IconSmall}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                    <Setter Property="Source" Value="pack://application:,,,/Resources/ContentBrowser/FolderSmall.png"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="LargeIconStyle" TargetType="{x:Type Image}">
            <Setter Property="Source" Value="{Binding Icon}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                    <Setter Property="Source" Value="pack://application:,,,/Resources/ContentBrowser/FolderLarge.png"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Taken from (Visual Studio Community installation path)\DesignTools\SystemThemes\Wpf\aero2.normalcolor.xaml
             (and edited so that it fits here)                                                  
        -->
        <Style x:Key="GridViewColumnHeaderGripper" TargetType="{x:Type Thumb}">
            <Setter Property="Canvas.Right" Value="-9"/>
            <Setter Property="Width" Value="18"/>
            <Setter Property="Height" Value="{Binding Path=ActualHeight, RelativeSource={RelativeSource TemplatedParent}}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush4}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <Rectangle HorizontalAlignment="Center" Width="1" Fill="{TemplateBinding Background}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="{x:Type GridViewColumnHeader}" TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush2}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="2,0"/>
            <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                        <Grid SnapsToDevicePixels="true">
                            <Border Name="HeaderBorder" BorderThickness="0,1"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}">
                                <TextBlock x:Name="HeaderContent" Margin="0,0,0,1" Text="{TemplateBinding Content}"
                                           VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                           HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                           SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                           TextTrimming="CharacterEllipsis"/>
                            </Border>
                            <Canvas>
                                <Thumb x:Name="PART_HeaderGripper" Style="{StaticResource GridViewColumnHeaderGripper}"/>
                            </Canvas>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="HeaderBorder" Property="Background" Value="{StaticResource Editor.Window.GrayBrush4}"/>
                                <Setter TargetName="PART_HeaderGripper" Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="HeaderBorder" Property="Background"
                                        Value="{StaticResource Editor.Window.GrayBrush6}"/>
                                <Setter TargetName="PART_HeaderGripper" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="HeaderContent" Property="Margin" Value="1,1,0,0"/>
                            </Trigger>
                            <Trigger Property="Height" Value="Auto">
                                <Setter Property="MinHeight" Value="20"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <EventSetter Event="Click" Handler="OnGridViewColumnHeader_Click"/>

            <Style.Triggers>
                <Trigger Property="Role" Value="Floating">
                    <Setter Property="Opacity" Value="0.4082"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                <Canvas Name="PART_FloatingHeaderCanvas">
                                    <Rectangle Opacity="0.4697" Fill="#FF000000" 
                                               Width="{TemplateBinding ActualWidth}" 
                                               Height="{TemplateBinding ActualHeight}"/>
                                </Canvas>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
                <Trigger Property="Role" Value="Padding">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="{x:Type GridViewColumnHeader}">
                                <Border Name="HeaderBorder" BorderThickness="0"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="{TemplateBinding Background}"/>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="Height" Value="Auto">
                                        <Setter Property="MinHeight" Value="20"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>


        <!-- Taken from (Visual Studio Community installation path)\DesignTools\SystemThemes\Wpf\aero2.normalcolor.xaml
             (and edited so that it fits here)                                                  
        -->
        <Style TargetType="{x:Type ListViewItem}">
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="5,2,5,2"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListViewItem}">
                        <Border BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}"
                            Background="{TemplateBinding Background}" CornerRadius="0" SnapsToDevicePixels="true">

                            <GridViewRowPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="{StaticResource Editor.Selected.BackgroundColor}" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>

                            </Trigger>
                            <Trigger Property="IsSelected" Value="true">
                                <Setter Property="Background" Value="{StaticResource Editor.Selected.BackgroundBrush}"/>
                            </Trigger>
                            
                            <!-- When item is selected but selection is not active -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="Selector.IsSelectionActive" Value="false"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush4}"/>
                            </MultiTrigger>
                            
                            <!-- When mouse hovers selected item -->
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="IsSelected" Value="true"/>
                                    <Condition Property="IsMouseOver" Value="true"/>
                                </MultiTrigger.Conditions>
                                <Setter Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="{StaticResource Editor.Selected.BackgroundColor}" Opacity="0.5"/>
                                    </Setter.Value>
                                </Setter>
                            </MultiTrigger>
                        
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <EventSetter Event="MouseDoubleClick" Handler="OnContent_Item_MouseDoubleClick"/>
            <EventSetter Event="KeyDown" Handler="OnContent_Item_KeyDown"/>

        </Style>

        <GridView x:Key="gridView">
            <!-- Icon and file name -->
            <GridViewColumn>
                <GridViewColumn.Header>
                    <GridViewColumnHeader Tag="FileName" Content="Name"/>
                </GridViewColumn.Header>

                <GridViewColumn.CellTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <Image Style="{StaticResource SmallIconStyle}" Width="18" Margin="5,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding FileName}" Margin="5,2,0,2" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                        </DockPanel>
                    </DataTemplate>
                </GridViewColumn.CellTemplate>
            </GridViewColumn>

            <!-- Date modified -->
            <GridViewColumn>
                <GridViewColumn.Header>
                    <GridViewColumnHeader Tag="DateModified" Content="Date modified"/>
                </GridViewColumn.Header>

                <GridViewColumn.CellTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding DateModified, StringFormat=dd-MMM-yy HH:mm:ss}" Margin="5,2,0,2" 
                                   VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                    </DataTemplate>
                </GridViewColumn.CellTemplate>
            </GridViewColumn>

            <!-- File size -->
            <GridViewColumn DisplayMemberBinding="{Binding Size, Converter={StaticResource dataSizeToStringConverter}}">
                <GridViewColumn.Header>
                    <GridViewColumnHeader Tag="Size" Content="Size"/>
                </GridViewColumn.Header>


            </GridViewColumn>

        </GridView>


        <!-- To understand more refer to: https://learn.microsoft.com/en-us/dotnet/desktop/wpf/controls/how-to-create-a-custom-view-mode-for-a-listview 
            for some wierd ahh Microslop reasons Style for tileView has to be in MageEditor/Themes/Generic.xaml 
             figure why \_(:)_/; Probably has to do with some sort of preloading by WPF so it knows what default style to apply 
             Though strange af, since I was able to hold everything under MageEditor/Dictionaries/ColorTemplates.xaml -->
        <DataTemplate x:Key="tileTemplate">
            <StackPanel Width="96" HorizontalAlignment="Center" Margin="-4,0,0,0">
                <Image Style="{StaticResource LargeIconStyle}" Width="90" HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding FileName}" TextTrimming="CharacterEllipsis" TextWrapping="Wrap"
                           Foreground="{StaticResource Editor.FontBrush}" HorizontalAlignment="Center"
                           Margin="2,0" MaxHeight="60"/>
            </StackPanel>
        </DataTemplate>
        
        <local:PlainView x:Key="tileView" ItemTemplate="{StaticResource tileTemplate}" ItemWidth="100">
            <local:PlainView.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}" BasedOn="{StaticResource ListBoxItemStyleDefault}">
                    <EventSetter Event="MouseDoubleClick" Handler="OnContent_Item_MouseDoubleClick"/>
                    <EventSetter Event="KeyDown" Handler="OnContent_Item_KeyDown"/>
                </Style>
            </local:PlainView.ItemContainerStyle>
        </local:PlainView>

        <Style TargetType="{x:Type ToggleButton}" x:Key="ListViewToggleButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource Editor.Window.GrayBrush4}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="ToolTip" Value="Grid View"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <Image x:Name="image" Source="pack://application:,,,/Resources/ContentBrowser/GridView.png"
                                   Width="16" Opacity="0.8"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Source" TargetName="image" Value="pack://application:,,,/Resources/ContentBrowser/TileView.png"/>
                                <Setter Property="ToolTip" TargetName="border" Value="Tile View"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource Editor.MouseOver.BorderBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </UserControl.Resources>
    
    <Grid Background="{StaticResource Editor.Window.GrayBrush2}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- IsTextSearchEnabled and TextSearch.TextPath make possible to navigate between items by pressing first few letters of a file's name -->
        <ListView ItemsSource="{Binding FolderContent}" Grid.Row="1" x:Name="folderListView"
                  IsTextSearchEnabled="True" TextSearch.TextPath="FileName">
            <ListView.Style>
                <Style TargetType="{x:Type ListView}">
                    <Setter Property="Background" Value="{StaticResource Editor.Window.GrayBrush2}"/>
                    <Setter Property="ListBox.BorderThickness" Value="0"/>
                    <Setter Property="View" Value="{StaticResource gridView}"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Path=IsChecked, ElementName=listViewToggle}" Value="False">
                            <Setter Property="View" Value="{StaticResource tileView}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
        </ListView>
        
        <Border Background="{StaticResource Editor.Window.GrayBrush3}" VerticalAlignment="Top">
            <Border.Effect>
                <DropShadowEffect Opacity="0.3" BlurRadius="10" ShadowDepth="0"/>
            </Border.Effect>

            <DockPanel>
                <DockPanel Margin="5" DockPanel.Dock="Top">
                    <ToggleButton DockPanel.Dock="Right" x:Name="listViewToggle" Style="{StaticResource ListViewToggleButtonStyle}"/>
                    <!-- placeholder for searching functionality, irrelevant for now -->
                    <TextBox />
                </DockPanel>
                <StackPanel Orientation="Horizontal" x:Name="pathStack" Margin="0,0,0,5">
                    <StackPanel.Resources>
                        <!-- Basically a style for buttons that show our path, which is basically slightly overridden version of default button style
                             from Dictionaries/ControlTemplates.xaml
                             + EventHandler for clicking these buttons so we can go back in hirarchy of files of our Content folder in project.
                        -->
                        <Style TargetType="{x:Type Button}" x:Key="{x:Type Button}">
                            <Setter Property="Margin" Value="2,1"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource Editor.FontBrush}"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="2,1"/>
                            <Setter Property="SnapsToDevicePixels" Value="True"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="MinWidth" Value="48"/>
                            <Setter Property="MaxHeight" Value="128"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        
                                        <Grid SnapsToDevicePixels="True">
                                            <!-- color should be changed in triggers -->
                                            <Border x:Name="borderSelected" SnapsToDevicePixels="True" CornerRadius="1"
                                                    BorderThickness="0" BorderBrush="Transparent"/>

                                            <ContentPresenter x:Name="contentPresenter" Focusable="False" 
                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                                              Margin="{TemplateBinding Padding}" RecognizesAccessKey="True" 
                                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>

                                        </Grid>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="White"/>
                                                <Setter Property="Background" TargetName="borderSelected" Value="{StaticResource Editor.LightPurpleBrush}"/>
                                                <Setter Property="Opacity" TargetName="borderSelected" Value="0.4"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="TextElement.Foreground" TargetName="contentPresenter" Value="White"/>
                                                <Setter Property="Background" TargetName="borderSelected" Value="{StaticResource Editor.LightPurpleBrush}"/>
                                                <Setter Property="Opacity" TargetName="borderSelected" Value="1"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>

                            <EventSetter Event="Click" Handler="OnPathStack_Button_Click"/>
                        </Style>

                        <Style TargetType="{x:Type Path}" x:Key="{x:Type Path}">
                            <Setter Property="Margin" Value="0,3,0,0"/>
                            <Setter Property="Data" Value="M0,0 L1.5,0 L4.5,3 L1.5,6 L0,6 L3,3 z"/>
                            <Setter Property="Fill" Value="{StaticResource Editor.FontBrush}"/>
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                        </Style>
                    </StackPanel.Resources>
                    <Image Source="pack://application:,,,/Resources/ContentBrowser/FolderSmall.png" Width="18" Margin="5,0,0,2"/>
                </StackPanel>
            </DockPanel>
            
        </Border>
    </Grid>
</UserControl>
